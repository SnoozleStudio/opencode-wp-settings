---
description: Build a feature from scratch or extend an existing one. Grills for alignment when scope is ambiguous, plans, implements, verifies.
---

Build: $ARGUMENTS

If the ask is ambiguous (new section, new feature, unclear scope), run a grilling session first (grill-me skill) to align scope, data sources (ACF fields?), and behavior. Then: explore the area, state the plan (files touched, blast radius), implement matching the house pattern (wp-plugin / wp-theme skill conventions), and run the verification chain before reporting done. For 3+ independent pieces, use the maestro agent to fan out.
