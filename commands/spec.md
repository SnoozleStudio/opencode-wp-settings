---
description: Turn the discussed feature into a written spec document.
---

Write a spec for: $ARGUMENTS

Use the to-spec skill. Synthesize what we've discussed into: goal (one sentence, project vocabulary), context, scope (in/out), behavior (user story + checkable acceptance criteria), WordPress contract (templates, ACF fields, hooks, enqueues, i18n), verification steps, risks. Ask at most ONE clarifying question if a hole blocks the spec. Keep it under ~120 lines.
