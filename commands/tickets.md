---
description: Break a plan or spec into small tracer-bullet tickets with declared blocking edges.
---

Create tickets for: $ARGUMENTS

Use the to-tickets skill. Break the work into vertical slices, each with: blocked-by/blocks edges, files it may touch, and checkable acceptance mapped to the verification chain. Serialize tickets that share files. Write them to a local ticket file (or GitHub issues if the repo is linked and requested). Output the file path + execution order (what first, what parallel).
