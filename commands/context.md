---
description: Build or sharpen the project's CONTEXT.md domain glossary and ADR log (with docs, also records architecture decisions).
---

Build/refresh the project context: $ARGUMENTS

Use the grill-with-docs skill (new projects / fuzzy vocabulary) or domain-modeling skill (existing projects): read CONTEXT.md and docs/adr/ if present, challenge terms against the code, stress-test with edge cases, and update CONTEXT.md (one-line term definitions) + ADRs inline (context/decision/consequences). Flag code that contradicts the model — propose renames, don't do them unilaterally.
