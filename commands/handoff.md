---
description: Save a handoff document so another session or agent can continue. Include current state, files, decisions, next steps.
---

Save a handoff for this session (done for today / continuing later): $ARGUMENTS

Use the handoff skill. Write handoff.md with: goal (project vocabulary), state (done/in-progress/blocked), files touched, decisions (link ADRs), next steps (ordered, with verification chain each must pass), open questions for the user, learnings for the log. Include branch, git status summary, and any pre-continuation commands. Keep it to one page.
